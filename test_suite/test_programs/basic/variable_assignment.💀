vibe main

yeet "vibez"

no cap
Variable Assignment Test
Tests: Variable declarations and assignments
Expected: Identical behavior in both modes
on god

slay main_character() {
    vibez.spill("=== Variable Assignment Test ===")
    
    sus x normie = 42
    vibez.spill("x =")
    vibez.spill(x)
    
    sus y normie = x + 8
    vibez.spill("y = x + 8 =")
    vibez.spill(y)
    
    sus name tea = "CURSED"
    vibez.spill("name =")
    vibez.spill(name)
    
    vibez.spill("=== Test Complete ===")
}
