vibe main
yeet "vibez"

no cap
Test: Basic Hello World program
Purpose: Simplest possible CURSED program
Expected: Should parse without any errors
on god

slay main_character() {
    fr fr Just basic variable assignment and output
    sus message tea = "Hello, CURSED World!"
    sus number normie = 42
    sus flag lit = based
    
    vibez.spill(message)
    vibez.spill(number)
    vibez.spill(flag)
    
    damn
}
