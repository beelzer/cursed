vibe main

yeet "vibez"

no cap
Nested Pointers Test
Tests: Pointers to pointers (double indirection)
Expected: Identical results in both modes
on god

slay main_character() {
    vibez.spill("=== Nested Pointers Test ===")
    
    sus x normie = 42
    sus ptr ඞnormie = ඞx
    sus ptrptr ඞඞnormie = ඞptr
    
    vibez.spill("Value through double pointer:")
    vibez.spill(**ptrptr)
    
    fr fr Modify through double pointer
    **ptrptr = 777
    
    vibez.spill("Original value after double pointer modification:")
    vibez.spill(x)
    
    vibez.spill("Value through single pointer:")
    vibez.spill(*ptr)
    
    vibez.spill("Value through double pointer:")
    vibez.spill(**ptrptr)
    
    vibez.spill("=== Test Complete ===")
}
