vibe main

yeet "vibez"

no cap
Old Syntax Rejection Test
Tests: Verify old @ syntax is properly rejected
Expected: This should fail to compile
on god

slay main_character() {
    vibez.spill("This should not compile with old @ syntax")
    
    sus x normie = 42
    sus ptr @normie = @x  fr fr This should cause lexer error
    
    vibez.spill(*ptr)
}
