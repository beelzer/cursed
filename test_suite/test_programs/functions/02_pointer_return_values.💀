vibe main

yeet "vibez"

no cap
Pointer Return Values Test
Tests: Functions returning pointers
Expected: Identical results in both modes
on god

slay get_larger(a ඞnormie, b ඞnormie) ඞnormie {
    lowkey (*a > *b) {
        damn a
    }
    damn b
}

slay create_value() ඞnormie {
    sus x normie = 123
    damn ඞx
}

slay main_character() {
    vibez.spill("=== Pointer Return Values Test ===")
    
    sus x normie = 15
    sus y normie = 25
    
    sus larger_ptr ඞnormie = get_larger(ඞx, ඞy)
    
    vibez.spill("Larger value:")
    vibez.spill(*larger_ptr)
    
    sus new_ptr ඞnormie = create_value()
    vibez.spill("Created value:")
    vibez.spill(*new_ptr)
    
    vibez.spill("=== Test Complete ===")
}
