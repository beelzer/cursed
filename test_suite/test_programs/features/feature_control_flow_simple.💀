vibe main

yeet "vibez"

slay main_character() {
    vibez.spill("=== Control Flow Test ===")
    
    sus x normie = 10
    lowkey x > 5 {
        vibez.spill("x is greater than 5")
    }
    
    sus grade normie = 85
    lowkey grade >= 90 {
        vibez.spill("Grade: A")
    } highkey {
        vibez.spill("Grade: B or below")
    }
    
    vibez.spill("=== Test Complete ===")
}
