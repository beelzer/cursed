vibe main

yeet "vibez"

no cap
Test: Validation of all CURSED Gen Z slang keywords
Purpose: Ensure all language keywords are recognized and parsed correctly
Expected: All keywords should parse without errors
on god

slay main_character() {
    fr fr Variable declarations using sus
    sus x normie = 42
    sus y tea = "no cringe"
    sus z lit = based
    
    fr fr Function call test
    sus result normie = calculate_something(x)
    
    fr fr Control flow with CURSED keywords
    lowkey x > 0 {
        vibez.spill("positive vibes")
    } highkey {
        vibez.spill("negative energy")
    }
    
    fr fr Return void - no value needed
    damn
}

slay calculate_something(value normie) normie {
    fr fr Basic arithmetic to test operators
    sus doubled normie = value * 2
    damn doubled + 10
}
