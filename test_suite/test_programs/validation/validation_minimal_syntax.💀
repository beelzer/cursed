vibe main

no cap
Test: Minimal CURSED syntax validation
Purpose: Test basic parsing without stdlib dependencies
Expected: Should parse and execute basic operations
on god

slay main_character() {
    fr fr Basic variable declarations
    sus x normie = 42
    sus y normie = 10
    sus z normie = x + y
    
    fr fr Simple control flow
    lowkey x > y {
        sus result normie = x * 2
    } highkey {
        sus result normie = y * 2
    }
    
    fr fr Simple arithmetic
    sus sum normie = x + y + z
    sus product normie = x * y
    
    damn 0
}
