vibe main
yeet "vibez"

no cap
    LeetCode #94: Binary Tree Inorder Traversal
    Classic tree traversal using Among Us pointers!
    Time: O(n), Space: O(n) where n is number of nodes
on god

squad TreeNode {
    sus val normie
    sus left ඞTreeNode
    sus right ඞTreeNode
}

slay inorder_traversal(root ඞTreeNode) normie[] {
    lowkey (root == nah) {
        damn []normie{}
    }

    sus result normie[] = []normie{}

    fr fr Left subtree
    sus left_arr normie[] = inorder_traversal(root.left)
    bestie i := 0; i < len(left_arr); i++ {
        result = append(result, left_arr[i])
    }

    fr fr Root node
    result = append(result, root.val)

    fr fr Right subtree
    sus right_arr normie[] = inorder_traversal(root.right)
    bestie i := 0; i < len(right_arr); i++ {
        result = append(result, right_arr[i])
    }

    damn result
}

slay create_tree() ඞTreeNode {
    no cap
        Create test tree: [1,null,2,3]
              1
               \
                2
               /
              3
    on god

    sus root ඞTreeNode = &TreeNode{val: 1, left: nah, right: nah}
    root.right = &TreeNode{val: 2, left: nah, right: nah}
    root.right.left = &TreeNode{val: 3, left: nah, right: nah}

    damn root
}

slay test_inorder_traversal() {
    vibez.spill("=== LeetCode #94: Binary Tree Inorder Traversal ===")

    fr fr Test case 1: [1,null,2,3]
    sus root1 ඞTreeNode = create_tree()
    sus result1 normie[] = inorder_traversal(root1)
    vibez.spill("Test 1 - Tree: [1,null,2,3]")
    vibez.spill("Inorder (expected [1,3,2]):", result1)

    fr fr Test case 2: []
    sus root2 ඞTreeNode = nah
    sus result2 normie[] = inorder_traversal(root2)
    vibez.spill("Test 2 - Empty tree:")
    vibez.spill("Inorder:", result2)

    fr fr Test case 3: [1]
    sus root3 ඞTreeNode = &TreeNode{val: 1, left: nah, right: nah}
    sus result3 normie[] = inorder_traversal(root3)
    vibez.spill("Test 3 - Single node [1]:")
    vibez.spill("Inorder:", result3)

    fr fr Test case 4: Full binary tree [4,2,6,1,3,5,7]
    sus root4 ඞTreeNode = &TreeNode{val: 4, left: nah, right: nah}
    root4.left = &TreeNode{val: 2, left: nah, right: nah}
    root4.right = &TreeNode{val: 6, left: nah, right: nah}
    root4.left.left = &TreeNode{val: 1, left: nah, right: nah}
    root4.left.right = &TreeNode{val: 3, left: nah, right: nah}
    root4.right.left = &TreeNode{val: 5, left: nah, right: nah}
    root4.right.right = &TreeNode{val: 7, left: nah, right: nah}
    sus result4 normie[] = inorder_traversal(root4)
    vibez.spill("Test 4 - BST [4,2,6,1,3,5,7]:")
    vibez.spill("Inorder (expected [1,2,3,4,5,6,7]):", result4)

    vibez.spill("=== Inorder Traversal Complete! Trees are sus but in a good way ===")
}

slay main_character() {
    test_inorder_traversal()
}
