vibe main
yeet "vibez"

slay test_return(x normie) normie {
    lowkey x > 0 {
        damn 1;
    } highkey lowkey x < 0 {
        damn -1;
    } highkey {
        damn 0;
    };
}

slay main_character() {
    vibez.spill("Testing with semicolons")
    sus result normie = test_return(5)
    vibez.spill(result)
}
