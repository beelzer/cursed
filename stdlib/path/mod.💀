vibe path

fr fr Path manipulation module - functions provided by native builtins
fr fr Available: join(a, b), split(path), basename(path), dirname(path),
fr fr   absolute(path), exists(path), is_dir(path), is_file(path)
