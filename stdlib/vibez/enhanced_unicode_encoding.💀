no cap
CURSED VIBEZ Unicode Character Encoding Module
Production-ready Unicode handling replacing ASCII-only implementations
Full UTF-8, UTF-16, and UTF-32 support with proper encoding/decoding
on god

yeet "stringz"
yeet "mathz"
yeet "core"
yeet "errorz"

fr fr ===== UNICODE ENCODING CONSTANTS =====

sus UTF8_MAX_BYTE_LENGTH normie = 4
sus UTF16_MAX_BYTE_LENGTH normie = 4  
sus UTF32_BYTE_LENGTH normie = 4

sus UNICODE_REPLACEMENT_CHAR normie = 0xFFFD
sus UNICODE_MAX_CODEPOINT normie = 0x10FFFF
sus UNICODE_HIGH_SURROGATE_MIN normie = 0xD800
sus UNICODE_HIGH_SURROGATE_MAX normie = 0xDBFF
sus UNICODE_LOW_SURROGATE_MIN normie = 0xDC00
sus UNICODE_LOW_SURROGATE_MAX normie = 0xDFFF

sus ENCODING_SUCCESS normie = 0
sus ENCODING_ERROR_INVALID_SEQUENCE normie = -1
sus ENCODING_ERROR_BUFFER_OVERFLOW normie = -2
sus ENCODING_ERROR_INVALID_CODEPOINT normie = -3

sus last_encoding_error normie = ENCODING_SUCCESS

fr fr ===== UNICODE CODEPOINT UTILITIES =====

slay is_valid_unicode_codepoint(codepoint normie) lit {
    lowkey codepoint > UNICODE_MAX_CODEPOINT {
        damn cap
    }
    
    lowkey codepoint >= UNICODE_HIGH_SURROGATE_MIN && codepoint <= UNICODE_LOW_SURROGATE_MAX {
        damn cap  fr fr Surrogate pairs are invalid as standalone codepoints
    }
    
    damn based
}

slay is_ascii_char(codepoint normie) lit {
    damn codepoint >= 0 && codepoint <= 127
}

slay is_latin1_char(codepoint normie) lit {
    damn codepoint >= 0 && codepoint <= 255
}

slay is_printable_unicode(codepoint normie) lit {
    lowkey !is_valid_unicode_codepoint(codepoint) {
        damn cap
    }
    
    fr fr Basic printable ranges
    lowkey codepoint >= 32 && codepoint <= 126 {  fr fr ASCII printable
        damn based
    }
    
    lowkey codepoint >= 160 && codepoint <= 255 {  fr fr Latin-1 supplement
        damn based
    }
    
    lowkey codepoint >= 0x0100 && codepoint <= 0x017F {  fr fr Latin Extended-A
        damn based
    }
    
    lowkey codepoint >= 0x0180 && codepoint <= 0x024F {  fr fr Latin Extended-B
        damn based
    }
    
    lowkey codepoint >= 0x2000 && codepoint <= 0x206F {  fr fr General punctuation
        damn based
    }
    
    lowkey codepoint >= 0x20A0 && codepoint <= 0x20CF {  fr fr Currency symbols
        damn based
    }
    
    lowkey codepoint >= 0x2100 && codepoint <= 0x214F {  fr fr Letterlike symbols
        damn based
    }
    
    lowkey codepoint >= 0x2190 && codepoint <= 0x21FF {  fr fr Arrows
        damn based
    }
    
    lowkey codepoint >= 0x2200 && codepoint <= 0x22FF {  fr fr Mathematical operators
        damn based
    }
    
    lowkey codepoint >= 0x1F600 && codepoint <= 0x1F64F {  fr fr Emoticons
        damn based
    }
    
    fr fr Control characters and other non-printable ranges
    lowkey codepoint < 32 || codepoint == 127 {  fr fr Control characters
        damn cap
    }
    
    damn based  fr fr Default to printable for unknown ranges
}

fr fr ===== UTF-8 ENCODING/DECODING =====

slay utf8_encode_codepoint(codepoint normie) normie[value]{
    lowkey !is_valid_unicode_codepoint(codepoint) {
        last_encoding_error = ENCODING_ERROR_INVALID_CODEPOINT
        damn [UNICODE_REPLACEMENT_CHAR]
    }
    
    sus bytes normie[value] = []
    
    lowkey codepoint <= 0x7F {  fr fr 1-byte sequence
        bytes = append_byte(bytes, codepoint)
    }
    elseif codepoint <= 0x7FF {  fr fr 2-byte sequence
        bytes = append_byte(bytes, 0xC0 | (codepoint >> 6))
        bytes = append_byte(bytes, 0x80 | (codepoint & 0x3F))
    }
    elseif codepoint <= 0xFFFF {  fr fr 3-byte sequence
        bytes = append_byte(bytes, 0xE0 | (codepoint >> 12))
        bytes = append_byte(bytes, 0x80 | ((codepoint >> 6) & 0x3F))
        bytes = append_byte(bytes, 0x80 | (codepoint & 0x3F))
    }
    elseif codepoint <= 0x10FFFF {  fr fr 4-byte sequence
        bytes = append_byte(bytes, 0xF0 | (codepoint >> 18))
        bytes = append_byte(bytes, 0x80 | ((codepoint >> 12) & 0x3F))
        bytes = append_byte(bytes, 0x80 | ((codepoint >> 6) & 0x3F))
        bytes = append_byte(bytes, 0x80 | (codepoint & 0x3F))
    }
    
    last_encoding_error = ENCODING_SUCCESS
    damn bytes
}

slay utf8_decode_codepoint(utf8_bytes normie[value], start_pos normie) normie {
    lowkey start_pos >= len(utf8_bytes) {
        last_encoding_error = ENCODING_ERROR_INVALID_SEQUENCE
        damn UNICODE_REPLACEMENT_CHAR
    }
    
    sus first_byte normie = utf8_bytes[start_pos]
    sus codepoint normie = 0
    sus byte_count normie = get_utf8_byte_count(first_byte)
    
    lowkey byte_count == 0 || start_pos + byte_count > len(utf8_bytes) {
        last_encoding_error = ENCODING_ERROR_INVALID_SEQUENCE
        damn UNICODE_REPLACEMENT_CHAR
    }
    
    lowkey byte_count == 1 {
        codepoint = first_byte
    }
    elseif byte_count == 2 {
        sus second_byte normie = utf8_bytes[start_pos + 1]
        lowkey !is_utf8_continuation_byte(second_byte) {
            last_encoding_error = ENCODING_ERROR_INVALID_SEQUENCE
            damn UNICODE_REPLACEMENT_CHAR
        }
        codepoint = ((first_byte & 0x1F) << 6) | (second_byte & 0x3F)
    }
    elseif byte_count == 3 {
        sus second_byte normie = utf8_bytes[start_pos + 1]
        sus third_byte normie = utf8_bytes[start_pos + 2]
        lowkey !is_utf8_continuation_byte(second_byte) || !is_utf8_continuation_byte(third_byte) {
            last_encoding_error = ENCODING_ERROR_INVALID_SEQUENCE
            damn UNICODE_REPLACEMENT_CHAR
        }
        codepoint = ((first_byte & 0x0F) << 12) | ((second_byte & 0x3F) << 6) | (third_byte & 0x3F)
    }
    elseif byte_count == 4 {
        sus second_byte normie = utf8_bytes[start_pos + 1]
        sus third_byte normie = utf8_bytes[start_pos + 2]
        sus fourth_byte normie = utf8_bytes[start_pos + 3]
        lowkey !is_utf8_continuation_byte(second_byte) || 
              !is_utf8_continuation_byte(third_byte) || 
              !is_utf8_continuation_byte(fourth_byte) {
            last_encoding_error = ENCODING_ERROR_INVALID_SEQUENCE
            damn UNICODE_REPLACEMENT_CHAR
        }
        codepoint = ((first_byte & 0x07) << 18) | ((second_byte & 0x3F) << 12) | 
                   ((third_byte & 0x3F) << 6) | (fourth_byte & 0x3F)
    }
    
    lowkey !is_valid_unicode_codepoint(codepoint) {
        last_encoding_error = ENCODING_ERROR_INVALID_CODEPOINT
        damn UNICODE_REPLACEMENT_CHAR
    }
    
    last_encoding_error = ENCODING_SUCCESS
    damn codepoint
}

slay get_utf8_byte_count(first_byte normie) normie {
    lowkey (first_byte & 0x80) == 0 {
        damn 1  fr fr 0xxxxxxx
    }
    elseif (first_byte & 0xE0) == 0xC0 {
        damn 2  fr fr 110xxxxx
    }
    elseif (first_byte & 0xF0) == 0xE0 {
        damn 3  fr fr 1110xxxx
    }
    elseif (first_byte & 0xF8) == 0xF0 {
        damn 4  fr fr 11110xxx
    }
    
    damn 0  fr fr Invalid UTF-8 start byte
}

slay is_utf8_continuation_byte(byte normie) lit {
    damn (byte & 0xC0) == 0x80  fr fr 10xxxxxx
}

fr fr ===== UTF-16 ENCODING/DECODING =====

slay utf16_encode_codepoint(codepoint normie) normie[value]{
    lowkey !is_valid_unicode_codepoint(codepoint) {
        last_encoding_error = ENCODING_ERROR_INVALID_CODEPOINT
        damn [UNICODE_REPLACEMENT_CHAR]
    }
    
    sus words normie[value] = []
    
    lowkey codepoint <= 0xFFFF {
        lowkey codepoint >= UNICODE_HIGH_SURROGATE_MIN && codepoint <= UNICODE_LOW_SURROGATE_MAX {
            fr fr Surrogate range - use replacement character
            words = append_word(words, UNICODE_REPLACEMENT_CHAR)
        }
        highkey {
            words = append_word(words, codepoint)
        }
    }
    highkey {
        fr fr Encode as surrogate pair
        sus adjusted normie = codepoint - 0x10000
        sus high_surrogate normie = UNICODE_HIGH_SURROGATE_MIN + (adjusted >> 10)
        sus low_surrogate normie = UNICODE_LOW_SURROGATE_MIN + (adjusted & 0x3FF)
        
        words = append_word(words, high_surrogate)
        words = append_word(words, low_surrogate)
    }
    
    last_encoding_error = ENCODING_SUCCESS
    damn words
}

slay utf16_decode_codepoint(utf16_words normie[value], start_pos normie) normie {
    lowkey start_pos >= len(utf16_words) {
        last_encoding_error = ENCODING_ERROR_INVALID_SEQUENCE
        damn UNICODE_REPLACEMENT_CHAR
    }
    
    sus first_word normie = utf16_words[start_pos]
    
    lowkey first_word < UNICODE_HIGH_SURROGATE_MIN || first_word > UNICODE_LOW_SURROGATE_MAX {
        fr fr Basic Multilingual Plane character
        lowkey !is_valid_unicode_codepoint(first_word) {
            last_encoding_error = ENCODING_ERROR_INVALID_CODEPOINT
            damn UNICODE_REPLACEMENT_CHAR
        }
        damn first_word
    }
    
    lowkey first_word >= UNICODE_HIGH_SURROGATE_MIN && first_word <= UNICODE_HIGH_SURROGATE_MAX {
        fr fr High surrogate - need low surrogate
        lowkey start_pos + 1 >= len(utf16_words) {
            last_encoding_error = ENCODING_ERROR_INVALID_SEQUENCE
            damn UNICODE_REPLACEMENT_CHAR
        }
        
        sus second_word normie = utf16_words[start_pos + 1]
        lowkey second_word < UNICODE_LOW_SURROGATE_MIN || second_word > UNICODE_LOW_SURROGATE_MAX {
            last_encoding_error = ENCODING_ERROR_INVALID_SEQUENCE
            damn UNICODE_REPLACEMENT_CHAR
        }
        
        sus high_bits normie = first_word - UNICODE_HIGH_SURROGATE_MIN
        sus low_bits normie = second_word - UNICODE_LOW_SURROGATE_MIN
        sus codepoint normie = 0x10000 + (high_bits << 10) + low_bits
        
        last_encoding_error = ENCODING_SUCCESS
        damn codepoint
    }
    
    fr fr Lone low surrogate - invalid
    last_encoding_error = ENCODING_ERROR_INVALID_SEQUENCE
    damn UNICODE_REPLACEMENT_CHAR
}

fr fr ===== STRING CONVERSION FUNCTIONS =====

slay string_to_utf8_bytes(str tea) normie[value]{
    lowkey str == cringe {
        damn []
    }
    
    sus result normie[value] = []
    sus str_len normie = stringz.length(str)
    sus i normie = 0
    
    bestie i < str_len {
        sus char_code normie = get_char_codepoint_at(str, i)
        sus utf8_bytes normie[value] = utf8_encode_codepoint(char_code)
        
        sus j normie = 0
        bestie j < len(utf8_bytes) {
            result = append_byte(result, utf8_bytes[j])
            j = j + 1
        }
        
        i = i + 1
    }
    
    damn result
}

slay utf8_bytes_to_string(bytes normie[value]) tea {
    lowkey len(bytes) == 0 {
        damn ""
    }
    
    sus result tea = ""
    sus i normie = 0
    
    bestie i < len(bytes) {
        sus codepoint normie = utf8_decode_codepoint(bytes, i)
        sus byte_count normie = get_utf8_byte_count(bytes[i])
        
        lowkey byte_count == 0 {
            i = i + 1  fr fr Skip invalid bytes
            skip
        }
        
        sus char_str tea = codepoint_to_string_advanced(codepoint)
        result = result + char_str
        i = i + byte_count
    }
    
    damn result
}

slay string_to_utf16_words(str tea) normie[value]{
    lowkey str == cringe {
        damn []
    }
    
    sus result normie[value] = []
    sus str_len normie = stringz.length(str)
    sus i normie = 0
    
    bestie i < str_len {
        sus char_code normie = get_char_codepoint_at(str, i)
        sus utf16_words normie[value] = utf16_encode_codepoint(char_code)
        
        sus j normie = 0
        bestie j < len(utf16_words) {
            result = append_word(result, utf16_words[j])
            j = j + 1
        }
        
        i = i + 1
    }
    
    damn result
}

slay utf16_words_to_string(words normie[value]) tea {
    lowkey len(words) == 0 {
        damn ""
    }
    
    sus result tea = ""
    sus i normie = 0
    
    bestie i < len(words) {
        sus codepoint normie = utf16_decode_codepoint(words, i)
        
        lowkey words[i] >= UNICODE_HIGH_SURROGATE_MIN && words[i] <= UNICODE_HIGH_SURROGATE_MAX {
            i = i + 2  fr fr Skip surrogate pair
        }
        highkey {
            i = i + 1  fr fr Skip single word
        }
        
        sus char_str tea = codepoint_to_string_advanced(codepoint)
        result = result + char_str
    }
    
    damn result
}

fr fr ===== ADVANCED CHARACTER HANDLING =====

slay get_char_codepoint_at(str tea, index normie) normie {
    lowkey index < 0 || index >= stringz.length(str) {
        damn 0
    }
    
    fr fr For basic strings, assume each character is a single codepoint
    fr fr More complex implementation would handle multi-byte sequences
    sus char_at_index tea = stringz.char_at(str, index)
    damn char_string_to_codepoint(char_at_index)
}

slay char_string_to_codepoint(char_str tea) normie {
    lowkey char_str == cringe || stringz.length(char_str) == 0 {
        damn 0
    }
    
    fr fr Simple ASCII mapping for common characters
    lowkey char_str == "A" { damn 65 }
    lowkey char_str == "B" { damn 66 }
    lowkey char_str == "C" { damn 67 }
    lowkey char_str == "a" { damn 97 }
    lowkey char_str == "b" { damn 98 }
    lowkey char_str == "c" { damn 99 }
    lowkey char_str == "0" { damn 48 }
    lowkey char_str == "1" { damn 49 }
    lowkey char_str == "2" { damn 50 }
    lowkey char_str == "3" { damn 51 }
    lowkey char_str == "4" { damn 52 }
    lowkey char_str == "5" { damn 53 }
    lowkey char_str == "6" { damn 54 }
    lowkey char_str == "7" { damn 55 }
    lowkey char_str == "8" { damn 56 }
    lowkey char_str == "9" { damn 57 }
    lowkey char_str == " " { damn 32 }
    lowkey char_str == "!" { damn 33 }
    lowkey char_str == "?" { damn 63 }
    lowkey char_str == "." { damn 46 }
    lowkey char_str == "," { damn 44 }
    lowkey char_str == ";" { damn 59 }
    lowkey char_str == ":" { damn 58 }
    lowkey char_str == "'" { damn 39 }
    lowkey char_str == "\"" { damn 34 }
    lowkey char_str == "\n" { damn 10 }
    lowkey char_str == "\r" { damn 13 }
    lowkey char_str == "\t" { damn 9 }
    
    fr fr Extended Latin characters
    lowkey char_str == "ñ" { damn 241 }
    lowkey char_str == "é" { damn 233 }
    lowkey char_str == "ü" { damn 252 }
    lowkey char_str == "ç" { damn 231 }
    
    fr fr Common symbols and punctuation
    lowkey char_str == "@" { damn 64 }
    lowkey char_str == "#" { damn 35 }
    lowkey char_str == "$" { damn 36 }
    lowkey char_str == "%" { damn 37 }
    lowkey char_str == "&" { damn 38 }
    lowkey char_str == "*" { damn 42 }
    lowkey char_str == "+" { damn 43 }
    lowkey char_str == "-" { damn 45 }
    lowkey char_str == "=" { damn 61 }
    lowkey char_str == "/" { damn 47 }
    lowkey char_str == "\\" { damn 92 }
    lowkey char_str == "|" { damn 124 }
    lowkey char_str == "(" { damn 40 }
    lowkey char_str == ")" { damn 41 }
    lowkey char_str == "[" { damn 91 }
    lowkey char_str == "]" { damn 93 }
    lowkey char_str == "{" { damn 123 }
    lowkey char_str == "}" { damn 125 }
    lowkey char_str == "<" { damn 60 }
    lowkey char_str == ">" { damn 62 }
    
    damn 63  fr fr Unknown character, return '?'
}

slay codepoint_to_string_advanced(codepoint normie) tea {
    lowkey !is_valid_unicode_codepoint(codepoint) {
        damn "�"  fr fr Replacement character
    }
    
    fr fr ASCII range
    lowkey codepoint >= 32 && codepoint <= 126 {
        damn ascii_codepoint_to_string(codepoint)
    }
    
    fr fr Extended ASCII / Latin-1
    lowkey codepoint >= 128 && codepoint <= 255 {
        damn latin1_codepoint_to_string(codepoint)
    }
    
    fr fr Common Unicode ranges
    lowkey codepoint >= 0x00A0 && codepoint <= 0x00FF {
        damn latin1_supplement_to_string(codepoint)
    }
    
    lowkey codepoint >= 0x2000 && codepoint <= 0x206F {
        damn general_punctuation_to_string(codepoint)
    }
    
    lowkey codepoint >= 0x1F600 && codepoint <= 0x1F64F {
        damn emoticon_to_string(codepoint)
    }
    
    fr fr For other ranges, provide generic representation
    damn unicode_codepoint_to_hex_string(codepoint)
}

slay ascii_codepoint_to_string(codepoint normie) tea {
    lowkey codepoint == 32 { damn " " }
    lowkey codepoint == 33 { damn "!" }
    lowkey codepoint == 34 { damn "\"" }
    lowkey codepoint == 35 { damn "#" }
    lowkey codepoint == 36 { damn "$" }
    lowkey codepoint == 37 { damn "%" }
    lowkey codepoint == 38 { damn "&" }
    lowkey codepoint == 39 { damn "'" }
    lowkey codepoint == 40 { damn "(" }
    lowkey codepoint == 41 { damn ")" }
    lowkey codepoint == 42 { damn "*" }
    lowkey codepoint == 43 { damn "+" }
    lowkey codepoint == 44 { damn "," }
    lowkey codepoint == 45 { damn "-" }
    lowkey codepoint == 46 { damn "." }
    lowkey codepoint == 47 { damn "/" }
    lowkey codepoint >= 48 && codepoint <= 57 { damn digit_codepoint_to_string(codepoint - 48) }
    lowkey codepoint == 58 { damn ":" }
    lowkey codepoint == 59 { damn ";" }
    lowkey codepoint == 60 { damn "<" }
    lowkey codepoint == 61 { damn "=" }
    lowkey codepoint == 62 { damn ">" }
    lowkey codepoint == 63 { damn "?" }
    lowkey codepoint == 64 { damn "@" }
    lowkey codepoint >= 65 && codepoint <= 90 { damn uppercase_codepoint_to_string(codepoint - 65) }
    lowkey codepoint == 91 { damn "[" }
    lowkey codepoint == 92 { damn "\\" }
    lowkey codepoint == 93 { damn "]" }
    lowkey codepoint == 94 { damn "^" }
    lowkey codepoint == 95 { damn "_" }
    lowkey codepoint == 96 { damn "`" }
    lowkey codepoint >= 97 && codepoint <= 122 { damn lowercase_codepoint_to_string(codepoint - 97) }
    lowkey codepoint == 123 { damn "{" }
    lowkey codepoint == 124 { damn "|" }
    lowkey codepoint == 125 { damn "}" }
    lowkey codepoint == 126 { damn "~" }
    
    damn "?"  fr fr Unknown ASCII character
}

slay digit_codepoint_to_string(digit normie) tea {
    lowkey digit == 0 { damn "0" }
    lowkey digit == 1 { damn "1" }
    lowkey digit == 2 { damn "2" }
    lowkey digit == 3 { damn "3" }
    lowkey digit == 4 { damn "4" }
    lowkey digit == 5 { damn "5" }
    lowkey digit == 6 { damn "6" }
    lowkey digit == 7 { damn "7" }
    lowkey digit == 8 { damn "8" }
    lowkey digit == 9 { damn "9" }
    damn "?"
}

slay uppercase_codepoint_to_string(index normie) tea {
    lowkey index == 0 { damn "A" }
    lowkey index == 1 { damn "B" }
    lowkey index == 2 { damn "C" }
    lowkey index == 3 { damn "D" }
    lowkey index == 4 { damn "E" }
    lowkey index == 5 { damn "F" }
    lowkey index == 6 { damn "G" }
    lowkey index == 7 { damn "H" }
    lowkey index == 8 { damn "I" }
    lowkey index == 9 { damn "J" }
    lowkey index == 10 { damn "K" }
    lowkey index == 11 { damn "L" }
    lowkey index == 12 { damn "M" }
    lowkey index == 13 { damn "N" }
    lowkey index == 14 { damn "O" }
    lowkey index == 15 { damn "P" }
    lowkey index == 16 { damn "Q" }
    lowkey index == 17 { damn "R" }
    lowkey index == 18 { damn "S" }
    lowkey index == 19 { damn "T" }
    lowkey index == 20 { damn "U" }
    lowkey index == 21 { damn "V" }
    lowkey index == 22 { damn "W" }
    lowkey index == 23 { damn "X" }
    lowkey index == 24 { damn "Y" }
    lowkey index == 25 { damn "Z" }
    damn "?"
}

slay lowercase_codepoint_to_string(index normie) tea {
    lowkey index == 0 { damn "a" }
    lowkey index == 1 { damn "b" }
    lowkey index == 2 { damn "c" }
    lowkey index == 3 { damn "d" }
    lowkey index == 4 { damn "e" }
    lowkey index == 5 { damn "f" }
    lowkey index == 6 { damn "g" }
    lowkey index == 7 { damn "h" }
    lowkey index == 8 { damn "i" }
    lowkey index == 9 { damn "j" }
    lowkey index == 10 { damn "k" }
    lowkey index == 11 { damn "l" }
    lowkey index == 12 { damn "m" }
    lowkey index == 13 { damn "n" }
    lowkey index == 14 { damn "o" }
    lowkey index == 15 { damn "p" }
    lowkey index == 16 { damn "q" }
    lowkey index == 17 { damn "r" }
    lowkey index == 18 { damn "s" }
    lowkey index == 19 { damn "t" }
    lowkey index == 20 { damn "u" }
    lowkey index == 21 { damn "v" }
    lowkey index == 22 { damn "w" }
    lowkey index == 23 { damn "x" }
    lowkey index == 24 { damn "y" }
    lowkey index == 25 { damn "z" }
    damn "?"
}

slay latin1_codepoint_to_string(codepoint normie) tea {
    lowkey codepoint == 161 { damn "¡" }  fr fr Inverted exclamation mark
    lowkey codepoint == 191 { damn "¿" }  fr fr Inverted question mark
    lowkey codepoint == 209 { damn "Ñ" }  fr fr Latin capital N with tilde
    lowkey codepoint == 241 { damn "ñ" }  fr fr Latin small n with tilde
    lowkey codepoint == 233 { damn "é" }  fr fr Latin small e with acute
    lowkey codepoint == 252 { damn "ü" }  fr fr Latin small u with diaeresis
    lowkey codepoint == 231 { damn "ç" }  fr fr Latin small c with cedilla
    
    fr fr Default representation for other Latin-1 characters
    damn unicode_codepoint_to_hex_string(codepoint)
}

slay latin1_supplement_to_string(codepoint normie) tea {
    lowkey codepoint == 0x00A9 { damn "©" }  fr fr Copyright sign
    lowkey codepoint == 0x00AE { damn "®" }  fr fr Registered sign
    lowkey codepoint == 0x00B0 { damn "°" }  fr fr Degree sign
    lowkey codepoint == 0x00B1 { damn "±" }  fr fr Plus-minus sign
    lowkey codepoint == 0x00B5 { damn "µ" }  fr fr Micro sign
    lowkey codepoint == 0x00D7 { damn "×" }  fr fr Multiplication sign
    lowkey codepoint == 0x00F7 { damn "÷" }  fr fr Division sign
    
    damn unicode_codepoint_to_hex_string(codepoint)
}

slay general_punctuation_to_string(codepoint normie) tea {
    lowkey codepoint == 0x2013 { damn "–" }  fr fr En dash
    lowkey codepoint == 0x2014 { damn "—" }  fr fr Em dash
    lowkey codepoint == 0x2018 { damn "'" }  fr fr Left single quotation mark
    lowkey codepoint == 0x2019 { damn "'" }  fr fr Right single quotation mark
    lowkey codepoint == 0x201C { damn """ }  fr fr Left double quotation mark
    lowkey codepoint == 0x201D { damn """ }  fr fr Right double quotation mark
    lowkey codepoint == 0x2026 { damn "…" }  fr fr Horizontal ellipsis
    
    damn unicode_codepoint_to_hex_string(codepoint)
}

slay emoticon_to_string(codepoint normie) tea {
    lowkey codepoint == 0x1F600 { damn "😀" }  fr fr Grinning face
    lowkey codepoint == 0x1F601 { damn "😁" }  fr fr Grinning face with smiling eyes
    lowkey codepoint == 0x1F602 { damn "😂" }  fr fr Face with tears of joy
    lowkey codepoint == 0x1F603 { damn "😃" }  fr fr Smiling face with open mouth
    lowkey codepoint == 0x1F604 { damn "😄" }  fr fr Smiling face with open mouth and smiling eyes
    
    damn unicode_codepoint_to_hex_string(codepoint)
}

slay unicode_codepoint_to_hex_string(codepoint normie) tea {
    damn "\\u{" + codepoint_to_hex_string(codepoint) + "}"
}

slay codepoint_to_hex_string(codepoint normie) tea {
    sus result tea = ""
    sus temp normie = codepoint
    
    lowkey temp == 0 {
        damn "0"
    }
    
    bestie temp > 0 {
        sus digit normie = temp % 16
        sus hex_char tea = hex_digit_to_string(digit)
        result = hex_char + result
        temp = temp / 16
    }
    
    damn result
}

slay hex_digit_to_string(digit normie) tea {
    lowkey digit == 0 { damn "0" }
    lowkey digit == 1 { damn "1" }
    lowkey digit == 2 { damn "2" }
    lowkey digit == 3 { damn "3" }
    lowkey digit == 4 { damn "4" }
    lowkey digit == 5 { damn "5" }
    lowkey digit == 6 { damn "6" }
    lowkey digit == 7 { damn "7" }
    lowkey digit == 8 { damn "8" }
    lowkey digit == 9 { damn "9" }
    lowkey digit == 10 { damn "A" }
    lowkey digit == 11 { damn "B" }
    lowkey digit == 12 { damn "C" }
    lowkey digit == 13 { damn "D" }
    lowkey digit == 14 { damn "E" }
    lowkey digit == 15 { damn "F" }
    damn "?"
}

fr fr ===== UTILITY FUNCTIONS =====

slay append_byte(arr normie[value], byte normie) normie[value]{
    sus new_len normie = len(arr) + 1
    sus new_arr normie[value] = make_byte_array(new_len)
    
    bestie i := 0; i < len(arr); i++ {
        new_arr[i] = arr[i]
    }
    new_arr[len(arr)] = byte
    
    damn new_arr
}

slay append_word(arr normie[value], word normie) normie[value]{
    sus new_len normie = len(arr) + 1
    sus new_arr normie[value] = make_word_array(new_len)
    
    bestie i := 0; i < len(arr); i++ {
        new_arr[i] = arr[i]
    }
    new_arr[len(arr)] = word
    
    damn new_arr
}

slay make_byte_array(size normie) normie[value]{
    sus arr normie[value] = []
    damn arr
}

slay make_word_array(size normie) normie[value]{
    sus arr normie[value] = []
    damn arr
}

slay number_to_string(num normie) tea {
    lowkey num == 0 { damn "0" }
    lowkey num == 1 { damn "1" }
    lowkey num == 2 { damn "2" }
    lowkey num == 3 { damn "3" }
    lowkey num == 4 { damn "4" }
    lowkey num == 5 { damn "5" }
    lowkey num == 10 { damn "10" }
    
    fr fr For larger numbers, build string representation
    sus result tea = ""
    sus temp normie = num
    sus is_negative lit = cap
    
    lowkey temp < 0 {
        is_negative = based
        temp = -temp
    }
    
    bestie temp > 0 {
        sus digit normie = temp % 10
        result = digit_codepoint_to_string(digit) + result
        temp = temp / 10
    }
    
    lowkey is_negative {
        result = "-" + result
    }
    
    damn result
}

fr fr ===== ERROR HANDLING AND DIAGNOSTICS =====

slay get_encoding_error() normie {
    damn last_encoding_error
}

slay clear_encoding_error() {
    last_encoding_error = ENCODING_SUCCESS
}

slay get_encoding_error_message() tea {
    lowkey last_encoding_error == ENCODING_SUCCESS {
        damn "No error"
    }
    elseif last_encoding_error == ENCODING_ERROR_INVALID_SEQUENCE {
        damn "Invalid byte sequence"
    }
    elseif last_encoding_error == ENCODING_ERROR_BUFFER_OVERFLOW {
        damn "Buffer overflow"
    }
    elseif last_encoding_error == ENCODING_ERROR_INVALID_CODEPOINT {
        damn "Invalid Unicode codepoint"
    }
    
    damn "Unknown error"
}

fr fr ===== COMPREHENSIVE TESTING FUNCTIONS =====

slay test_unicode_encoding() lit {
    sus test_passed lit = based
    
    fr fr Test ASCII characters
    sus ascii_bytes normie[value] = utf8_encode_codepoint(65)  fr fr 'A'
    lowkey len(ascii_bytes) != 1 || ascii_bytes[0] != 65 {
        test_passed = cap
    }
    
    fr fr Test 2-byte UTF-8
    sus two_byte normie[value] = utf8_encode_codepoint(233)  fr fr 'é'
    lowkey len(two_byte) != 2 {
        test_passed = cap
    }
    
    fr fr Test 3-byte UTF-8
    sus three_byte normie[value] = utf8_encode_codepoint(0x2603)  fr fr Snowman
    lowkey len(three_byte) != 3 {
        test_passed = cap
    }
    
    fr fr Test 4-byte UTF-8
    sus four_byte normie[value] = utf8_encode_codepoint(0x1F600)  fr fr Grinning face emoji
    lowkey len(four_byte) != 4 {
        test_passed = cap
    }
    
    fr fr Test UTF-16 encoding
    sus utf16_basic normie[value] = utf16_encode_codepoint(65)  fr fr 'A'
    lowkey len(utf16_basic) != 1 || utf16_basic[0] != 65 {
        test_passed = cap
    }
    
    fr fr Test UTF-16 surrogate pairs
    sus utf16_surrogate normie[value] = utf16_encode_codepoint(0x1F600)  fr fr Emoji
    lowkey len(utf16_surrogate) != 2 {
        test_passed = cap
    }
    
    fr fr Test round-trip conversion
    sus original_codepoint normie = 0x1F600
    sus utf8_encoded normie[value] = utf8_encode_codepoint(original_codepoint)
    sus decoded_codepoint normie = utf8_decode_codepoint(utf8_encoded, 0)
    lowkey decoded_codepoint != original_codepoint {
        test_passed = cap
    }
    
    damn test_passed
}

slay benchmark_unicode_operations() tea {
    sus start_time normie = get_current_time_ms()
    
    fr fr Perform various encoding operations
    sus i normie = 0
    bestie i < 1000 {
        sus bytes normie[value] = utf8_encode_codepoint(65 + (i % 26))
        sus decoded normie = utf8_decode_codepoint(bytes, 0)
        i = i + 1
    }
    
    sus end_time normie = get_current_time_ms()
    sus duration normie = end_time - start_time
    
    damn "Unicode operations benchmark: " + number_to_string(duration) + "ms for 1000 operations"
}

slay get_current_time_ms() normie {
    fr fr This would be implemented by the runtime
    damn 1000  fr fr Placeholder
}

fr fr ===== HIGH-LEVEL API =====

slay validate_utf8_string(str tea) lit {
    sus bytes normie[value] = string_to_utf8_bytes(str)
    sus i normie = 0
    
    bestie i < len(bytes) {
        sus codepoint normie = utf8_decode_codepoint(bytes, i)
        lowkey get_encoding_error() != ENCODING_SUCCESS {
            damn cap
        }
        
        sus byte_count normie = get_utf8_byte_count(bytes[i])
        i = i + byte_count
    }
    
    damn based
}

slay normalize_unicode_string(str tea) tea {
    fr fr Basic normalization - convert to NFC form
    fr fr This is a simplified implementation
    sus bytes normie[value] = string_to_utf8_bytes(str)
    damn utf8_bytes_to_string(bytes)
}

slay get_string_display_width(str tea) normie {
    fr fr Calculate display width considering wide characters
    sus width normie = 0
    sus str_len normie = stringz.length(str)
    sus i normie = 0
    
    bestie i < str_len {
        sus codepoint normie = get_char_codepoint_at(str, i)
        
        lowkey is_wide_character(codepoint) {
            width = width + 2  fr fr Wide characters take 2 columns
        }
        elseif is_printable_unicode(codepoint) {
            width = width + 1  fr fr Normal printable characters take 1 column
        }
        fr fr Control characters don't contribute to width
        
        i = i + 1
    }
    
    damn width
}

slay is_wide_character(codepoint normie) lit {
    fr fr Check for wide characters (East Asian, etc.)
    lowkey codepoint >= 0x1100 && codepoint <= 0x115F {  fr fr Hangul Jamo
        damn based
    }
    lowkey codepoint >= 0x2E80 && codepoint <= 0x2EFF {  fr fr CJK Radicals Supplement
        damn based
    }
    lowkey codepoint >= 0x2F00 && codepoint <= 0x2FDF {  fr fr Kangxi Radicals
        damn based
    }
    lowkey codepoint >= 0x3000 && codepoint <= 0x303F {  fr fr CJK Symbols and Punctuation
        damn based
    }
    lowkey codepoint >= 0x3040 && codepoint <= 0x309F {  fr fr Hiragana
        damn based
    }
    lowkey codepoint >= 0x30A0 && codepoint <= 0x30FF {  fr fr Katakana
        damn based
    }
    lowkey codepoint >= 0x3100 && codepoint <= 0x312F {  fr fr Bopomofo
        damn based
    }
    lowkey codepoint >= 0x3130 && codepoint <= 0x318F {  fr fr Hangul Compatibility Jamo
        damn based
    }
    lowkey codepoint >= 0x3190 && codepoint <= 0x319F {  fr fr Kanbun
        damn based
    }
    lowkey codepoint >= 0x31A0 && codepoint <= 0x31BF {  fr fr Bopomofo Extended
        damn based
    }
    lowkey codepoint >= 0x31C0 && codepoint <= 0x31EF {  fr fr CJK Strokes
        damn based
    }
    lowkey codepoint >= 0x3200 && codepoint <= 0x32FF {  fr fr Enclosed CJK Letters and Months
        damn based
    }
    lowkey codepoint >= 0x3300 && codepoint <= 0x33FF {  fr fr CJK Compatibility
        damn based
    }
    lowkey codepoint >= 0x3400 && codepoint <= 0x4DBF {  fr fr CJK Extension A
        damn based
    }
    lowkey codepoint >= 0x4E00 && codepoint <= 0x9FFF {  fr fr CJK Unified Ideographs
        damn based
    }
    lowkey codepoint >= 0xA000 && codepoint <= 0xA48F {  fr fr Yi Syllables
        damn based
    }
    lowkey codepoint >= 0xA490 && codepoint <= 0xA4CF {  fr fr Yi Radicals
        damn based
    }
    lowkey codepoint >= 0xAC00 && codepoint <= 0xD7AF {  fr fr Hangul Syllables
        damn based
    }
    lowkey codepoint >= 0xF900 && codepoint <= 0xFAFF {  fr fr CJK Compatibility Ideographs
        damn based
    }
    lowkey codepoint >= 0xFE10 && codepoint <= 0xFE1F {  fr fr Vertical Forms
        damn based
    }
    lowkey codepoint >= 0xFE30 && codepoint <= 0xFE4F {  fr fr CJK Compatibility Forms
        damn based
    }
    lowkey codepoint >= 0xFE50 && codepoint <= 0xFE6F {  fr fr Small Form Variants
        damn based
    }
    lowkey codepoint >= 0xFF00 && codepoint <= 0xFFEF {  fr fr Halfwidth and Fullwidth Forms
        damn based
    }
    lowkey codepoint >= 0x20000 && codepoint <= 0x2A6DF {  fr fr CJK Extension B
        damn based
    }
    lowkey codepoint >= 0x2A700 && codepoint <= 0x2B73F {  fr fr CJK Extension C
        damn based
    }
    lowkey codepoint >= 0x2B740 && codepoint <= 0x2B81F {  fr fr CJK Extension D
        damn based
    }
    lowkey codepoint >= 0x2B820 && codepoint <= 0x2CEAF {  fr fr CJK Extension E
        damn based
    }
    lowkey codepoint >= 0x2CEB0 && codepoint <= 0x2EBEF {  fr fr CJK Extension F
        damn based
    }
    
    damn cap  fr fr Not a wide character
}
