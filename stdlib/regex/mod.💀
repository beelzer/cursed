vibe regex

fr fr Regular expressions module - functions provided by native builtins
fr fr Available: compile(pattern), match(pattern, str), find(pattern, str),
fr fr   find_all(pattern, str), replace(pattern, str, replacement),
fr fr   replace_all(pattern, str, replacement), split(pattern, str)
