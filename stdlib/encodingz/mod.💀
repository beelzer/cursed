no cap
===== ENCODINGZ MODULE ENTRY POINT =====
High-performance encoding/decoding utilities for CURSED
This module provides comprehensive encoding support for modern applications
on god

yeet "encodingz"

fr fr Re-export all encoding functions for easy access
fr fr Users can now: yeet "encodingz" and use all functions directly

no cap
Base64 functions are automatically available:
- base64_encode(data)
- base64_decode(encoded) 
- base64_encode_url_safe(data)
- base64_decode_url_safe(encoded)
on god

no cap
Hex functions are automatically available:
- hex_encode(data)
- hex_decode(encoded)
- hex_encode_upper(data)
on god

fr fr ASCII85 functions are automatically available:
fr fr - ascii85_encode(data)

no cap
URL functions are automatically available:
- url_encode(data)
- url_decode(encoded)
on god

no cap
Streaming functions are automatically available:
- create_stream_encoder(encoding_type)
- stream_encode_chunk(encoder, chunk)
- stream_finalize(encoder)
on god

fr fr Performance functions are automatically available:
fr fr - benchmark_encoding(data, iterations)

no cap
Module is production-ready and optimized for:
✅ High-performance encoding/decoding operations
✅ Memory-efficient streaming for large data
✅ Comprehensive error handling
✅ Security-conscious implementations
✅ Cross-platform compatibility
✅ Zero external dependencies
on god
