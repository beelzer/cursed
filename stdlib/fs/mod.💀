vibe fs

fr fr Filesystem module - functions provided by native builtins
fr fr Available: read_file(path), write_file(path, content), file_exists(path),
fr fr   create_dir(path), is_dir(path), get_file_size(path),
fr fr   append_file(path, content), delete_file(path), list_dir(path)
