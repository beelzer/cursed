vibe stringz

fr fr String operations module - functions provided by native builtins
fr fr Available: length(str), len(str), concat(a, b), from_int(n), from_number(n),
fr fr   char_at(str, idx), from_char(code), replace_all(str, old, new),
fr fr   to_upper(str), to_lower(str), upper(str), lower(str),
fr fr   substring(str, start, end), contains(str, search),
fr fr   split(str, delim), trim(str), starts_with(str, prefix), ends_with(str, suffix),
fr fr   index_of(str, search), repeat(str, count), reverse(str), to_number(str)
