vibe memory

fr fr Memory management module - functions provided by native builtins
fr fr Available: malloc(size), free(ptr), realloc(ptr, size),
fr fr   memset(ptr, value, size), memcpy(dest, src, size),
fr fr   get_memory_stats(), gc_collect()
